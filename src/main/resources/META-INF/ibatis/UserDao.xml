<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
"http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="UserDao">
  <select id="getUserById" resultClass="com.j.stock.vo.User">
    select * from user where userId = #userId#
  </select>
  
  <insert id="addUser" parameterClass="com.j.stock.vo.User">
  	insert into user 
  	(userName, password, userTel)
  	values 
  	(#userName#, #password#, #userTel#)
  	<selectKey resultClass="String" keyProperty="userId">  
        select LAST_INSERT_ID() as userId  
    </selectKey>  
  </insert>
  
  <update id="updateUser" parameterClass="com.j.stock.vo.User">
  	update user
  	set password = #password#, userTel = #userTel#
  	where userId = #userId#
  </update>
  
  <delete id="deleteUser">
  	delete from user where userId = #userId#
  </delete>
</sqlMap>